import React from "react";
import Loader from "react-loader-spinner";

const Spinner = () => {
  return <Loader type="Oval" color="#1a6652" width={50} height={50} />;
};

export default Spinner;
